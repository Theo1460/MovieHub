/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['image.tmdb.org'], // Ajout du domaine autorisé pour charger les images
    },
  };
  
  export default nextConfig;
  